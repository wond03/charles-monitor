---
AIGC:
    Label: "1"
    ContentProducer: 001191440300708461136T1XGW3
    ProduceID: c1ba86f5d2bfc4f956728a8b00cc41c2_90a98acbb21f11f1ab3f52540024e231
    ReservedCode1: qskH425N3cSdqP2eY6PAPEYRHVfu5tJInb3xNXnzNNz/DEoYb4U4eeiTntld6dKCJFmHshKWiiTlRT3KkYsqHteH5uQMkS5X9KRtRHdU15KIlrErMT5cW+KQ+aJwcCmTYHVN7CG4RFllQr6DW0ipuKTvy0O6ExMXp1IywD8GJTKA1X2SBxL1ZNf0er4=
    ContentPropagator: 001191440300708461136T1XGW3
    PropagateID: c1ba86f5d2bfc4f956728a8b00cc41c2_90a98acbb21f11f1ab3f52540024e231
    ReservedCode2: qskH425N3cSdqP2eY6PAPEYRHVfu5tJInb3xNXnzNNz/DEoYb4U4eeiTntld6dKCJFmHshKWiiTlRT3KkYsqHteH5uQMkS5X9KRtRHdU15KIlrErMT5cW+KQ+aJwcCmTYHVN7CG4RFllQr6DW0ipuKTvy0O6ExMXp1IywD8GJTKA1X2SBxL1ZNf0er4=
---

# 零服务器部署指南：GitHub Actions 免费云端监控

不需要常驻电脑、不需要买服务器。用 GitHub 的免费云机器每 15 分钟自动扫描一次 BTC / 黄金，
命中查尔斯策略信号就推送企业微信。全程免费。已按日内交易模式调优：15M 级别确认，扫描节奏对齐 15M K线收线。

---

## 一、准备一个 GitHub 账号（没有的话注册一个，免费）

打开 https://github.com 注册即可。

## 二、新建私有仓库（Private）

1. 登录后点右上角 `+` → `New repository`
2. Repository name 填：`charles-monitor`
3. Visibility 选 **Private**（私有，别人看不到你的代码）
4. 勾选 `Add a README file` → `Create repository`

## 三、上传代码

把 `查尔斯信号监控系统` 文件夹里的**所有内容**（包括 `.github` 隐藏文件夹）上传到仓库根目录。

网页上传方式（最简单，不用装 Git）：

1. 打开仓库首页 → 点 `Add file` → `Upload files`
2. 把下面这些文件拖进去：
   ```
   .github/workflows/monitor.yml   ← 隐藏文件夹，Windows 上先显示隐藏文件再拖
   config.yaml
   datafeed.py
   monitor.py
   pusher.py
   requirements.txt
   signal_engine.py
   test_signal.py
   README.md
   ```
3. 底部点 `Commit changes`

## 四、把企业微信 webhook 存进 Secrets（安全存放，不出现在代码里）

1. 仓库页面点 `Settings` → 左侧 `Secrets and variables` → `Actions`
2. 点 `New repository secret`
3. Name 填：`WECOM_WEBHOOK`
4. Secret 填：`https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=9dfe55d7-466f-4a34-b5dd-5d86e7251b24`
5. 点 `Add secret`

## 五、手动测试一次（立刻验证）

1. 点仓库顶部 `Actions` 标签
2. 左侧点 `charles-monitor` → 右侧 `Run workflow` 按钮
3. 等 1-2 分钟，绿色勾 = 成功，企业微信会收到一条推送

## 六、自动运行

配置里的定时任务 `*/15 * * * *` 会自动生效（UTC 时间，每 15 分钟一次；北京时间 = UTC+8）。
同时每次 push 代码也会触发一次扫描，上传后即可看到结果。
以后每次运行都能在 `Actions` 页面看到执行记录，失败会有红叉提示。

---

## 常见问题

**Q：免费额度够用吗？**
A：私有仓库每月 2000 分钟。当前 15 分钟跑一次 + pip 缓存（每次约 20 秒）≈ 每月 2880 次 ≈ 960 分钟/月，余量充足。
想更省就改 `monitor.yml` 里的 `cron: '*/15 * * * *'` 为 `'*/30 * * * *'`。

**Q：想要更快的扫描频率？**
A：GitHub 免费版 schedule 最小间隔是 5 分钟。私有仓库额度有限，想更快就把仓库改成 Public（公开），
Actions 完全不限分钟数（webhook 在 Secrets 里依然保密，不会泄露）。

**Q：扫描间隔会不会错过信号？**
A：日内模式下信号确认以 15M 级别为主（1H 数结构定方向 + 15M 确认进场），15 分钟扫描节奏正好对齐 15M K线收线，
信号出现后最多延迟 15 分钟内推送，不会错过。

**Q：国内访问 GitHub 不稳定怎么办？**
A：GitHub Actions 是 GitHub 的服务器在跑，不受你本地网络影响；
只有你"上传代码"和"查看执行记录"时需要访问 github.com，偶尔慢属正常。

---

## 备选方案

- **手机 Termux（Android）**：手机装 Termux App，把脚本放进去 `python monitor.py` 直接跑，
  适合不折腾 GitHub 但手机能保持联网后台常驻的老板（注意部分手机会杀后台）。
- **腾讯云函数 SCF**：国内云，配置稍复杂，免费额度充足，适合后续进阶。
*（内容由AI生成，仅供参考）*
